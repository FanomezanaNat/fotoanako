CREATE TABLE "user"
(
    d BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    last_name VARCHAR(255),
    first_name VARCHAR(255),
    email VARCHAR(255),
    agenda_id BIGINT REFERENCES agenda(id) ON DELETE SET NULL
);